const { db } = require('../data/db')

function listProjects(req, res) {}

function getProjects(req, res) {}

function createProject(req, res) {}

module.exports = { listProjects, getProjects, createProject };